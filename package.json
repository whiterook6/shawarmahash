{
  "name": "shawarmahash",
  "version": "1.0.0",
  "description": "",
  "license": "UNLICENSED",
  "author": "whiterook6",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node18 --format=esm --outfile=build/index.js --packages=external",
    "dev": "esbuild src/index.ts --bundle --platform=node --target=node18 --format=esm --outfile=build/index.js --sourcemap --packages=external"
  },
  "private": true,
  "dependencies": {
    "@fastify/sse": "^0.4.0",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.3"
  }
}
